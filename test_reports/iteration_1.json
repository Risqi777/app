{
  "summary": "Backend smoke + integration testing on shift-scheduler API. Executed 27 checks across auth, personil CRUD, schedule generation/cell upsert, requests (approve overrides schedule cells, reject leaves schedule intact), settings, and stats. All checks passed on public URL.",
  "tested_scope": ["backend", "auth", "integration"],
  "passed": [
    "root /api/",
    "admin_login (seeded credentials)",
    "login_wrong_password_401",
    "login_wrong_nik_401",
    "register_personil (returns token)",
    "duplicate_email_400",
    "duplicate_nik_400",
    "GET /api/auth/me (role=admin)",
    "POST /api/personil by admin",
    "POST /api/personil by personil -> 403",
    "PATCH /api/personil/{id}",
    "PATCH /api/settings by personil -> 403",
    "POST /api/schedule/generate (created cells > 0)",
    "POST /api/schedule/generate by personil -> 403",
    "GET /api/schedule?month=&year=",
    "POST /api/schedule/cell upsert",
    "POST /api/requests (personil creates own pending)",
    "PATCH /api/requests/{id} approved (auto-overrides 3 schedule cells with request type)",
    "PATCH /api/requests/{id} rejected (no schedule modification)",
    "GET /api/requests personil sees only own",
    "GET /api/requests admin sees all",
    "GET/PATCH /api/settings (title/logo base64)",
    "GET /api/stats (total_personil, pending_requests, approved_requests, today_counts)",
    "DELETE /api/personil/{id}"
  ],
  "failed": [],
  "action_items": [],
  "success_rate": {"backend": "100%", "frontend": "not tested (skipped per request)"},
  "retest_needed": false,
  "full_testing_deferred": true,
  "notes": "Test script at /app/test_reports/backend_test.py. Personil created per-run with unique email/NIK to avoid collisions across iterations. Frontend testing skipped as requested."
}
